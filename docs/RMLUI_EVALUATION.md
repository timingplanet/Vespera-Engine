# RmlUi evaluation archive

The 0.9.7 side-by-side evaluation **passed on Windows** and is no longer the active architectural decision. Vespera 0.9.8 adopts RmlUi as the low-level production UI foundation while keeping Vespera-owned APIs/rendering/assets/editor/MCP boundaries and retaining `.slui` v3 for compatibility during migration.

See `RMLUI.md` for the current architecture and limitations.
